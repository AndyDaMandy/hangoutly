<%= form_with(model: hangout) do |form| %>
  <% if hangout.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hangout.errors.count, "error") %> prohibited this hangout from being saved:</h2>

      <ul>
        <% hangout.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :date, style: "display: block" %>
    <%= form.date_field :date %>
  </div>

  <div>
    <%= form.label :time, style: "display: block" %>
    <%= form.time_field :time %>
  </div>

  <div>
    <%= form.label :creator_id, style: "display: block" %>
    <%= form.number_field :creator_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
